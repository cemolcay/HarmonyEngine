# HarmonyEngine

`HarmonyEngine` is an extraction-ready library layer that sits above `MusicTheory`.

Current status:

- source and tests are scaffolded under this folder
- package wiring is intentionally deferred
- implementation depends on `MusicTheory` only

Planned package layout:

```text
HarmonyEngine/
  Sources/HarmonyEngine/
  Tests/HarmonyEngineTests/
```

